# 模块一交付包

基线日期2026-09-15，单独执行tests/module1：199项、188通过、0失败、11跳过。指定三模块语句与分支综合覆盖79.43%。

- 测试用例清单.xlsx：按附录1字段整理199项执行用例，包含方法、输入、预期、实际结果及源码节点。
- 缺陷清单.docx：模块一3项缺陷及修复前后证据。
- 测试报告.docx：按附录3六章结构填写，包含8个典型用例和成员代号。
- 成果汇报.pptx：10页，可编辑，供5至8分钟汇报。
- evidence/：模块一日志、HTML、JUnit、覆盖率、源码指纹、Git记录。
- 演示方案待讨论.md：仅列备选方案和待定项，未录制视频或进行现场演练。

## CUDA

当前11项CUDA用例跳过。对照仓库2026-09-13历史日志，同项均为SKIPPED，无已完成CUDA通过结果可采用。
研究训练日志不能替代这些单元测试的执行结果；后续由小组补测，不计为通过。

## 贡献统计口径

代码比例按当前HEAD已提交且仍存在的SteelRailWay目录Python行进行git blame：成员A（EdwinNull）28179行、100%；成员B（sundowner）0行、0%；成员C（无记录）0行、0%。
统计包括Python注释与空行，不含二进制、模型和未提交修改；不等同于实际课程总工作量。
全仓库提交记录A为5次、B为3次，其中B含1次合并；B的清理维护工作有记录但不能据此虚构新增代码。
个人缺陷数、其他工作量与成员C贡献需小组据实复核；未提交AI辅助内容保留独立说明。

## 复现

在仓库根目录，使用D:\Anaconda\envs\AI_common\python.exe运行：

```powershell
& 'D:\Anaconda\envs\AI_common\python.exe' -m pytest tests/module1/ --cov --cov-config=.coveragerc --cov-report=term-missing
```

交付件仍待小组复核代号与贡献；视频形式未定。未将模块二结果混入模块一。
